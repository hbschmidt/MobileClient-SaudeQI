<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.elrondsadvice.saudeqi">

    <application
        android:allowBackup="true"
        android:icon="@drawable/sites_saude_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">
        </activity>

        <activity
            android:name=".Medico.PesquisaMedico"
            android:label="@string/title_activity_pesquisa_medico"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".perfilUsuario"
            android:label="@string/title_activity_perfil_usuario"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>